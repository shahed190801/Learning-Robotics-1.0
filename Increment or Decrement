// Variable Types
//gm to kg converter

void setup()
{
 Serial.begin(9600);
  int i;
  i=1;
 Serial.println(i);
  i++;  //i++ = i+1; we can use it for increment
        //i-- = i-1; we can use it for decrement
 Serial.print(i);
}

void loop()
{
  
}
